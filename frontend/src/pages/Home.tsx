import { Container } from "@/components";

const Home = () => {
  return (
    <div>
      <Container className="flex items-center justify-center">
        <h2>Home</h2>
      </Container>
    </div>
  );
};

export default Home;
